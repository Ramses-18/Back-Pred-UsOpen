spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-lingering-tooth-atffslnk-pooler.c-9.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_BEnS6QOutx3a&sslmode=require&channelBinding=require}
    username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
    password: ${SPRING_DATASOURCE_PASSWORD:18042004}
    driver-class-name: org.postgresql.Driver


jpa:
  hibernate:
    ddl-auto: update
  show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true

      
  sql:
    init:
      mode: never # Cambiado a 'never' para evitar que falle por scripts SQL antiguos

server:
  port: ${PORT:8081}

app:
  jwt:
    secret: ${JWT_SECRET:cambiar-este-secreto-en-produccion-minimo-256-bits-requerido-1234567890}
    expiration-ms: 86400000
  deadline-offset-minutes: 30

cors:
  allowed-origins: ${CORS_ORIGINS:https://front-wimbledon-2dgb.vercel.app/}
